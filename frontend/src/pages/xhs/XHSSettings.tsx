/**
 * XHS Settings — Module configuration.
 */
export default function XHSSettings() {
    return (
        <div style={{ maxWidth: 900 }}>
            <div style={{ background: '#fff', borderRadius: 12, border: '1px solid #e8ecf1', padding: 60, textAlign: 'center', color: '#94a3b8' }}>
                <div style={{ fontSize: 40, marginBottom: 12 }}>⚙️</div>
                <div style={{ fontSize: 14, marginBottom: 8 }}>小红书模块设置</div>
                <div style={{ fontSize: 12 }}>AI 配置、发布配置、采集配置（开发中）</div>
            </div>
        </div>
    );
}
